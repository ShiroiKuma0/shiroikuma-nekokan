<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:description="@string/permissionReadCardsDescription"
        android:icon="@drawable/ic_launcher_monochrome"
        android:label="@string/permissionReadCardsLabel"
        android:name="${applicationId}.READ_CARDS"
        android:protectionLevel="dangerous" />

    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- 保存復元 automation v2: SkAutomationDataService streams a full card archive through a
         caller-supplied descriptor, which can take minutes. Its type is specialUse because none of
         the framework's categories describes "hand this app's own backup to another app". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="23" />
    <!-- 保存復元 automation: an EXPORT_STATE broadcast may name an absolute directory to write the
         backup ZIP into (白い熊's one archive folder), which needs All-files access. Granted by hand
         from the UI page's Export/Import section; never used by the app's own interactive flows. -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Without this, setPackage() on our reply and progress broadcasts fails SILENTLY on Android
         11+: the export runs, writes correctly, and is never heard of. Package visibility is not
         optional plumbing here — it is the difference between a working contract and one that looks
         like a dead app to the caller. -->
    <queries>
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".LoyaltyCardLockerApplication"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/sk_app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:localeConfig="@xml/locales_config">
        
        <!-- The 保存復元 state-export contract (SkStateExportReceiver): sister-app automation
             triggers this app's Export/Import backup headlessly. No android:permission — the caller
             cannot hold one. Since contract v2 the master switch ships ON and the token is opt-in,
             so this is the deliberately unauthenticated half of the surface: it only writes where
             it was told to and reports what it did. Note what that means for THIS app, whose
             archive is loyalty-card barcodes: with the token off, any app on the phone can name a
             path and trigger one. 「Use authorization token?」 in the UI page is how that is shut. -->
        <receiver
            android:name="protect.card_locker.shiroikuma.SkStateExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.action.EXPORT_STATE" />
                <action android:name="${applicationId}.action.LIST_CATEGORIES" />
                <!-- Fire-and-forget stop for a running export. It must reach us from outside, so it
                     is routed through this exported receiver rather than any internal component. -->
                <action android:name="${applicationId}.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>

        <!-- 保存復元 automation v2, the data door. Exported with no android:permission on purpose:
             a custom permission would have to be `normal` (any app may hold it) or `signature`
             (impossible — every app in this family has its own keystore, 42 of them). Neither buys
             anything SkAutomationCallers does not already do better, by checking the caller's exact
             package name, its uid as the kernel reports it, and its pinned signing certificate. -->
        <provider
            android:name="protect.card_locker.shiroikuma.SkAutomationProvider"
            android:authorities="${applicationId}.automation"
            android:exported="true" />

        <service
            android:name="protect.card_locker.shiroikuma.SkAutomationDataService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Automation data export/import" />
        </service>

        <!-- Capability discovery, readable WITHOUT waking this app — which matters because 白い熊
             freezes apps aggressively, and a disabled package cannot be asked anything. 応用管理
             reads these across every installed package at once with MATCH_UNINSTALLED_PACKAGES |
             MATCH_DISABLED_COMPONENTS. An app without them is simply not offered for data backup,
             so declaring them IS the opt-in. -->
        <meta-data android:name="shiroikuma.automation.contract" android:value="2" />
        <meta-data android:name="shiroikuma.automation.format" android:value="1" />
        <meta-data android:name="shiroikuma.automation.min_format" android:value="1" />

        <receiver
            android:name=".ListWidget"
            android:label="@string/card_list_widget_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/list_widget_info" />
         </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="content"/>
                <data android:host="*"/>
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/vnd.apple.pkpass" />
                <data android:mimeType="application/vnd-com.apple.pkpass" />
                <data android:mimeType="application/vnd.espass-espass" />
                <data android:mimeType="application/vnd.apple.pkpasses" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
                <data android:mimeType="image/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/vnd.apple.pkpass" />
                <data android:mimeType="application/vnd-com.apple.pkpass" />
                <data android:mimeType="application/vnd.espass-espass" />
                <data android:mimeType="application/vnd.apple.pkpasses" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/about"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ManageGroupsActivity"
            android:label="@string/groups"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ManageGroupActivity"
            android:label="@string/group_edit"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".cardview.LoyaltyCardViewActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".cardimageview.LoyaltyCardImageViewActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoyaltyCardEditActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter
                android:autoVerify="true"
                android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Main card sharing URIs -->
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data
                    android:host="@string/intent_import_card_from_url_host_catima_app"
                    android:pathPrefix="@string/intent_import_card_from_url_path_prefix_catima_app" />
            </intent-filter>
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Old card sharing URIs -->
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data
                    android:host="@string/intent_import_card_from_url_host_thelastproject"
                    android:pathPrefix="@string/intent_import_card_from_url_path_prefix_thelastproject" />
                <data
                    android:host="@string/intent_import_card_from_url_host_brarcher"
                    android:pathPrefix="@string/intent_import_card_from_url_path_prefix_brarcher" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScanActivity"
            android:label="@string/scanCardBarcode"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BarcodeSelectorActivity"
            android:label="@string/selectBarcodeTitle"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".preferences.SettingsActivity"
            android:label="@string/settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- shiroikuma-nekokan fork: the 白い熊 猫管 UI page -->
        <activity
            android:name=".shiroikuma.SkUiActivity"
            android:label="@string/sk_ui_title"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- FIXME: ImportExportActivity cancels import on rotation -->
        <activity
            android:name=".ImportExportActivity"
            android:label="@string/importExport"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- ZIP Intent Filter -->
            <intent-filter
                android:label="@string/importCards">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/zip" />
                <data android:scheme="content"/>
                <data android:host="*"/>
            </intent-filter>

            <!-- JSON Intent Filter -->
            <intent-filter
                android:label="@string/importCards">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
                <data android:scheme="content"/>
                <data android:host="*"/>
            </intent-filter>

            <!-- CSV Intent Filter -->
            <intent-filter
                android:label="@string/importCards">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content"/>
                <data android:host="*" />
                <data android:mimeType="text/comma-separated-values" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CardShortcutConfigure"
            android:exported="true"
            android:label="@string/cardShortcut"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UCropWrapper"
            android:theme="@style/AppTheme.NoActionBar" />

        <provider
            android:name=".contentprovider.CardsContentProvider"
            android:authorities="${applicationId}.contentprovider.cards"
            android:exported="true"
            android:readPermission="${applicationId}.READ_CARDS"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
        <service android:name=".CardsOnPowerScreenService" android:label="@string/app_name"
            android:permission="android.permission.BIND_CONTROLS" android:exported="true"
            tools:targetApi="r">
            <intent-filter>
                <action android:name="android.service.controls.ControlsProviderService" />
            </intent-filter>
        </service>

        <service
            android:name=".wearos.BluetoothServerService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <activity
            android:name=".wearos.BluetoothPairingActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.Transparent" />

        <receiver
            android:name=".wearos.BluetoothPairingReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="protect.card_locker.wearos.action.ALLOW_DEVICE" />
                <action android:name="protect.card_locker.wearos.action.BLOCK_DEVICE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
